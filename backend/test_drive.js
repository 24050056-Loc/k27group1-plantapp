const fs = require('fs'); const { google } = require('googleapis'); async function test() { const content = fs.readFileSync('token.json'); const credentials = JSON.parse(content); const client = google.auth.fromJSON(credentials); const drive = google.drive({ version: 'v3', auth: client }); try { const res = await drive.files.list({ pageSize: 1, fields: 'files(id, name)' }); console.log(res.data); } catch (err) { console.error('ERROR:', err.message); } } test();
